# Lakshya Admin Frontend - Environment Variables
# For local development:
# VITE_API_URL is typically not needed because vite.config.js proxies /api to localhost:5000.
# 
# For Production (e.g. Vercel):
# You MUST specify the URL where your Render backend is hosted. 
VITE_API_URL=https://localhost:5000/api
